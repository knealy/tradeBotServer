# TopStepX Trading Bot - Git Ignore File

# Test data
*.csv

# Logs
*.log

# Temporary files
*_COMPLETE.txt
*_TO_ADD.txt

# Environment variables and secrets
.env
.env.backup
.env.local
.env.production
.env.staging
*.env.*
# Paths like docs/archive/old.env are not matched by *.env.*
docs/archive/old.env
docs/archive/old.env.*

# API credentials
credentials.json
api_keys.txt
secrets.txt

# Log files
*.log
logs/
trading_bot.log
trading_bot.log.*

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Trading data
data/
market_data/
positions.json
orders.json

# Cache files
.cache/
*.parquet
*.pkl

# CSV exports (generated on-demand)
*.csv

# Backup files
*.bak
*.backup
*.tmp

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# Jupyter Notebooks
.ipynb_checkpoints

# pyenv
.python-version
.account_state.json

# Local env/secrets (.env.example is the committed template)
.env
.env.local
.env.bak
.env.backup
.env.backup.*
.env.clean
.env.production
.env.staging
# Generated charts/logs
charts/
logs/
.gui_websocket_port

# Local Master GUI page theme overrides (optional; see docs/PAGE_THEME_OPTIONS.md)
config/page_theme.json

# Rust build artifacts (do NOT commit; previously had ~1700 files tracked)
rust/target/

# Alpha discovery — machine-readable outputs (markdown reports are committed; JSON and sensitivity grids are not)
docs/alpha/*.json
docs/alpha/sensitivity/

# Walk-forward competition — per-fold JSON outputs (regenerate with scripts/walkforward_strategy_competition.py)
docs/perf/walkforward_competition/runs/

# Parameter sweep outputs (regenerate with scripts/strategy_parameter_sweep.py)
docs/perf/parameter_sweeps/

# Walk-forward trade recap bundle (191+ HTML charts; regenerate with scripts/walkforward_trade_recap_report.py)
docs/perf/walkforward_mnq_mgc_3m_overnight_morning_trade_recaps/

# Decision cache for walkforward_trade_recap_report.py --cache-decisions
# (content-addressed replay payloads; safe to delete to force fresh runs).
docs/perf/_decision_cache/
