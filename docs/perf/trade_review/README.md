# Trade review outputs (generated)

HTML and PNG files in this directory are produced by:

`python scripts/render_trade_review_charts.py --json … --csv … --out-dir docs/perf/trade_review`

They are local artifacts (not required for the bot). Safe to delete or add to `.gitignore` if you prefer a clean tree.
